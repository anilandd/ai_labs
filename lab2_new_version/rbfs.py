def RBFS(init_state, goal_state):
    visited = []
    path = [init_state]
    # while path:
    #     cur_state = path[0]
    #     for state in path[1:]:
    #         if   
    if init_state == goal_state:
        print("Found!")
    else:
        print("Not found!")
    return None